<div class="project-card">
  <a href="{{ project.url | relative_url }}" class="card-link">
    <!-- IMAGE FIRST -->
    <div class="card-image-container">
      {% if project.preview_split %}
        <!-- Split view: same image shown twice with different focus -->
        <div class="split-preview">
          <div class="split-left">
            {% if project.preview_gif contains '.mp4' %}
              <video autoplay loop muted playsinline class="card-gif"
                     style="object-position: {{ project.preview_split.left | default: 'left' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
                <source src="{{ project.preview_gif | relative_url }}" type="video/mp4">
              </video>
            {% elsif project.preview_gif contains '.webm' %}
              <video autoplay loop muted playsinline class="card-gif"
                     style="object-position: {{ project.preview_split.left | default: 'left' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
                <source src="{{ project.preview_gif | relative_url }}" type="video/webm">
              </video>
            {% else %}
              <img src="{{ project.preview_gif | relative_url }}" 
                   alt="{{ project.title }} - left view" 
                   class="card-gif"
                   style="object-position: {{ project.preview_split.left | default: 'left' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
            {% endif %}
          </div>
          <div class="split-right">
            {% if project.preview_gif contains '.mp4' %}
              <video autoplay loop muted playsinline class="card-gif"
                     style="object-position: {{ project.preview_split.right | default: 'right' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
                <source src="{{ project.preview_gif | relative_url }}" type="video/mp4">
              </video>
            {% elsif project.preview_gif contains '.webm' %}
              <video autoplay loop muted playsinline class="card-gif"
                     style="object-position: {{ project.preview_split.right | default: 'right' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
                <source src="{{ project.preview_gif | relative_url }}" type="video/webm">
              </video>
            {% else %}
              <img src="{{ project.preview_gif | relative_url }}" 
                   alt="{{ project.title }} - right view" 
                   class="card-gif"
                   style="object-position: {{ project.preview_split.right | default: 'right' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
            {% endif %}
          </div>
        </div>
      {% else %}
        <!-- Standard single view -->
        {% if project.preview_gif contains '.mp4' %}
          <video autoplay loop muted playsinline class="card-gif"
                 style="object-position: {{ project.preview_position | default: 'center' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
            <source src="{{ project.preview_gif | relative_url }}" type="video/mp4">
          </video>
        {% elsif project.preview_gif contains '.webm' %}
          <video autoplay loop muted playsinline class="card-gif"
                 style="object-position: {{ project.preview_position | default: 'center' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
            <source src="{{ project.preview_gif | relative_url }}" type="video/webm">
          </video>
        {% else %}
          <img src="{{ project.preview_gif | relative_url }}" 
               alt="{{ project.title }}" 
               class="card-gif"
               style="object-position: {{ project.preview_position | default: 'center' }}; transform: scale({{ project.preview_zoom | default: 1 }});">
        {% endif %}
      {% endif %}
      <div class="card-overlay">
        <span class="view-project-text">View Project</span>
      </div>
    </div>

    <!-- CONTENT SECOND -->
    <div class="card-content">
      <h3 class="card-title">{{ project.title }}</h3>
      {% if project.subtitle %}
      <p class="card-subtitle">{{ project.subtitle }}</p>
      {% endif %}
      <div class="card-tags">
        {% for tag in project.tags %}
          <span class="tag">{{ tag }}</span>
        {% endfor %}
      </div>
    </div>
  </a>
</div>